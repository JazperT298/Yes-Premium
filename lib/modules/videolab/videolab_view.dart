import 'package:flutter/material.dart';

class VideoLabView extends StatelessWidget {
  const VideoLabView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Video Lab'),
      ),
    );
  }
}
