import 'package:flutter/material.dart';

class EducatorListView extends StatelessWidget {
  const EducatorListView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Educator List'),
      ),
    );
  }
}
